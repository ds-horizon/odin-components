{
  "flavour": "${FLAVOUR}",
  "stage": "${STAGE}",
  "config": {
    "stageName": "${STAGE}"
  },
  "stateConfig": {
    "provider": "S3",
    "uri": "s3://odin-components-state-stag/test/sh1/redis-sh-01-aws-k8s-redis.txt"
  }
}