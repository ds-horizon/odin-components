{
    "flavour": "aws_elasticache",
    "stage": "${STAGE}",
    "config": {
        "operationName": "${OPERATION_NAME}"
    },
    "stateConfig": {
      "provider": "S3",
      "uri": "https://components-state-odin-dsl.s3.amazonaws.com/aws_elasticache/${ENV_NAME}/${COMPONENT_NAME}"
    }
}
