{
  "title": "Schema valid for across all flavours",
  "properties": {
    "username": {
      "description": "MySQL database username for authentication and connection. Used by applications to connect and perform database operations. Changing this requires updating all client connections and service accounts. **Required:** Must match MySQL user privileges. **Production:** Use dedicated service accounts with least-privilege access.",
      "type": "string"
    },
    "password": {
      "description": "MySQL database password for authentication and access control. Secures database access and prevents unauthorized connections. Store securely in secrets management systems (e.g., Vault, AWS Secrets Manager). **Required:** Strong passwords recommended (16+ chars, mixed case, special chars). **Production:** Rotate regularly and use secrets management.",
      "type": "string"
    },
    "version": {
      "description": "MySQL version to be created",
      "type": "string",
      "enum": [
        "8.0"
      ]
    },
    "discovery": {
      "type": "object",
      "description": "Discovery config for the MySQL cluster",
      "properties": {
        "reader": {
          "description": "The private discovery endpoint for the reader",
          "type": "string",
          "pattern": "^[^A-Z]*$"
        },
        "writer": {
          "description": "The private discovery endpoint for the writer",
          "type": "string",
          "pattern": "^[^A-Z]*$"
        }
      },
      "additionalProperties": false,
      "required": [
        "reader",
        "writer"
      ]
    }
  },
  "required": [
    "version",
    "discovery"
  ],
  "additionalProperties": false
}
